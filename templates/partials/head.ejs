<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="<%= locals.description || '' %>">

  <title><%= typeof locals.title !== 'undefined' ? locals.title + ' - ' : '' %><%= locals.site ? locals.site.title : '' %></title>

  <!-- 预连接 CDN -->
  <link rel="preconnect" href="https://cdn.jsdelivr.net">

  <!-- Highlight.js 主题 -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/highlight.js@11.9.0/styles/<%= locals.config ? locals.config.highlight.theme : 'github-dark' %>.min.css">

  <!-- KaTeX 数学公式 -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/katex@0.16.9/dist/katex.min.css">

  <!-- Mermaid 流程图 -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/mermaid@10.7.0/dist/mermaid.min.css">

  <!-- 自定义样式 -->
  <link rel="stylesheet" href="<%= locals.base || '' %>/css/style.css">

  <!-- 搜索数据 -->
  <script>
    window.searchData = [];
    <% if (locals.posts) { %>
      <% locals.posts.forEach(function(post) { %>
        window.searchData.push({
          title: '<%- post.title %>',
          slug: '<%- post.slug %>',
          excerpt: '<%- post.excerpt.substring(0, 100) %>',
          tags: <%- JSON.stringify(post.tags || []) %>,
          categories: <%- JSON.stringify(post.categories || []) %>
        });
      <% }); %>
    <% } %>
  </script>
</head>
<body>
  <div class="layout">
